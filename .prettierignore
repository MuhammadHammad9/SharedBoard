# Files Prettier must not touch.
#
# Written after a repo-wide `pnpm format` rewrote all four categories below and
# produced a 228,000-line diff. Each exclusion is a correctness requirement,
# not a preference.

# ── The delivered specifications — R-PREC-020 (Blocking) ────────────────────
# These three are preserved EXACTLY as delivered and their integrity is
# verifiable. Corrections go in the defect register at RULES.md §2.4, never
# into the files. Reflowing their markdown is an edit, however cosmetic.
docs/01-PRD.md
docs/02-FLOWS.md
docs/03-TRD.md

# ── The stress fixture ──────────────────────────────────────────────────────
# Generated by scripts/generate-stress-board.ts as single-line JSON, and
# deliberately BYTE-STABLE across runs so diffs stay meaningful (PRD risk R-2).
# Pretty-printing it added 221,770 lines and grew it from 5.5 MB to 7.5 MB —
# and `pnpm fixtures:generate` would flip it straight back on the next run.
fixtures/

# ── Generated and vendored ──────────────────────────────────────────────────
pnpm-lock.yaml
dist/
dist-types/
coverage/
playwright-report/
test-results/
node_modules/
apps/server/prisma/migrations/
